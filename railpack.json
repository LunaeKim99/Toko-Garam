{
  "provider": "php",
  "steps": {
    "build": {
      "commands": [
        "npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "php artisan optimize:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
